# DSH batch review (c3129724..e6e773c0) — first-hand confirmed findings

## Global / release chain
- G1 [MAJOR] npm-publish.yml still tracked on master; references `working-directory: dsh-plugin` (dir moved to examples/harness-agnostic/dsh-plugin by #590). Deletion commit 77ed263f exists only on claude/release-unified (never squashed). Dead workflow; any manual dispatch fails.
- G2 [MAJOR] cli.py:523 — `"watch-list": "clawock.decision.watch_list",    "t0-review": ...` two dict entries on one line; also breaks alphabetical sort (w inserted between t and t).
- G3 [MINOR] Commit messages with literal `\n` (b48bc227 #590, 29d6ba4b #587).
- G4 [INFO] v0.1.3 tag created 00:07; release.yml run queued; PyPI/npm publish in flight (was missing at review start — transient).
- G5 [MINOR] memory/2026-08-15.md says "尚未 npm publish(缺凭据)" — npm clawock-dsh@0.1.0 actually published 2026-08-15T15:36:55Z (23:36 HKT), 0.1.3 in flight. Memory stale.
- G6 [MINOR] refresh_readme_metrics.py docstring cites deleted readme-metrics.yml; default --data-plane reads master's assets/data (stale vs data-plane branch) for manual runs.
- G7 [MINOR] Live checkout left on branch claude/release-0.1.3 (AGENTS.md: live checkout must stay on master).
- G8 [MINOR] instruments.json: +187 lines mostly theme-array reformatting mixed into feature PR; semantic diff buried.
- G9 [MAJOR] README.zh.md:153 — "区间约 42%–64%;高信心组 55%...区间约 38%–72%——全部跨过 50% 线" — CIs printed right there straddle 50% (42–64, 38–72), yet text claims "全部跨过 50% 线"; contradicts memory/2026-08-15.md 口径 (53%/55% CI 跨 50% 不能自己盖章); should read "点估计跨 50%,但区间包含 50%".

## Watchdog (#550/#569)
- W1 [MAJOR] alert_brief_missing: attempt-2 rerun queued then fallback dispatched unconditionally — concurrent writers to same artifacts (local vs GHA runner), cross-env delivery markers → duplicate WeChat/Telegram risk (matches #508 pattern).
- W2 [MAJOR] test_brief_watchdog_second_rerun.py:66/:76 `assert spy.get("reruns") or [] == []` — parses as `spy.get("reruns") or True`; always passes (VERIFIED).
- W3 [MAJOR] 09:05 rerun gated by counter, not by evidence (no cron_run_ended_in_failure check); logs unverified "the 08:00 run already ended in error".
- W4 [MAJOR] gateway CLI calls (2×120s timeout) inserted before fallback dispatch on critical path, contradicting module's own "alert must not be delayed by gateway" rule.
- W5 [MINOR] counter flag format migration ISO→int unhandled (old timestamp → count 0); non-atomic write_text.
- W6 [MINOR] schedule-unreadable path silent (no log); alert/follow-up only report fallback, not attempt-2 rerun; could falsely claim "no auto recovery" if local rerun succeeded.

## Signals/plans/safe_io
- S1 [MAJOR] compute_short_history_signals falls back for ANY 20-29 bar name in refresh_rows/provisional_setups — weakens the deliberate 30-bar data-quality gate for mature names with partial feeds (no new-listing check); tag "短历史" is the only tell.
- S2 [MAJOR] plans.py overridden_by_user key has zero consumers / zero skill/doc mentions — contract added without the documentation half.
- S3 [MINOR] _load_json duplicated helper in intraday_preflight; inline try/except for add-alpha-policy.json repeats pattern instead of using it; safe_io.load_json_cached third JSON-read style in same batch.
- S4 [MINOR] cross-module private call quant_signals._universe_details(); defensive getattr for compute_short_history_signals that exists in the same release.
- S5 [MINOR] import ordering inconsistencies (signals.py, intraday_delta.py, plans.py: workspace before decision, non-alpha append).
- S6 [INFO] safe_io.load_json_cached: mtime-keyed, unbounded cache, shared mutable objects; OK for CLI lifetime; mtime ns-precision assumption.

## Intraday features (to be refined by subagent A)
- I1 [MAJOR] collect_early_trend_candidates vs collect_opportunity_radar ~85% duplicated loops in same file.
- I2 [MINOR] OPPORTUNITY_NEAR_PCT=5.0 duplicates watch_list.NEAR_HIGH_PCT=5.0 (cross-module policy constant drift risk).
- I3 [INFO] intraday_delta docstring now says "only for a condition delta" — reprice/forced-review wording removed; verify code matches (grep found no reprice in file → docstring fix is accurate).
