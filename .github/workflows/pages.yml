name: Deploy GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - '.github/workflows/pages.yml'
      - '_config.yml'
      - '_layouts/**'
      - 'index.html'
      - '4fb2df1611ed42e5b67fd6171a237acb.txt'
      - 'briefs.md'
      - 'README.md'
      - 'README.zh.md'
      - 'robots.txt'
      - 'manifest.webmanifest'
      - 'docs/**'
      - 'assets/css/**'
      - 'assets/js/**'
      - 'assets/data/**'
      - 'assets/*.png'
      - 'assets/*.svg'
      - 'assets/icons/**'
      - 'memory/*-pre-open.md'
      - 'memory/weekly/**'
      - 'config/pages-public.json'
      - 'scripts/build/prepare_pages_artifact.py'
  pull_request:
    paths:
      - '.github/workflows/pages.yml'
      - '_config.yml'
      - '_layouts/**'
      - 'index.html'
      - '4fb2df1611ed42e5b67fd6171a237acb.txt'
      - 'briefs.md'
      - 'README.md'
      - 'README.zh.md'
      - 'robots.txt'
      - 'manifest.webmanifest'
      - 'docs/**'
      - 'assets/css/**'
      - 'assets/js/**'
      - 'assets/data/**'
      - 'assets/*.png'
      - 'assets/*.svg'
      - 'assets/icons/**'
      - 'memory/*-pre-open.md'
      - 'memory/weekly/**'
      - 'config/pages-public.json'
      - 'scripts/build/prepare_pages_artifact.py'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # The four dashboard outputs are published to an orphan data branch, not
      # to master (#314). Jekyll builds _site from the checkout, so they have to
      # be in the checkout first. Loud on failure: a fall back to whatever the
      # checkout happened to carry is how a site serves a stale generation with
      # every gate still green.
      - name: Fetch the published generation
        run: python3 scripts/build/fetch_data_plane.py

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: Prepare consumer-safe artifact
        run: >-
          python3 scripts/build/prepare_pages_artifact.py
          --site-dir _site
          --output-dir _pages

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./_pages

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
