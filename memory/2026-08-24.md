# 2026-08-24

## Overview 首屏五项整改 + 黄金回本（PR #940，已合并 dca9c282，refresh_live 已快进本机）

kcn 口头批注拆五条 + 黄金一条，单 PR 交付：

1. 柱图溢出：`dailyBars` 落点 `(i+0.5)/n` 槽位居中（原 `i/n` 首柱中心压容器左缘、半根悬出）。粗细封顶 9px 不变。新闸：390/900/1440 柱体 bbox 必须在 `.rc-bars` 内。
2. 毛玻璃（DSH 参照）：判定卡 `--glass-card` 55% + blur24 saturate170；body 主光 620→860px（#887 判据：先证明背后有光可糊）；reduced-transparency 回退实色（`.card.overview-verdict` 双类名赢特异度）。
3. 四格只填 3 格 = hero-rail（#927 搬走「今日」后剩三格）：补「距峰值」格（同 spark 序列的峰→现 + 自最低收复量表 + 峰值说明），桌面 4 列 / 移动 2×2 补满；地板 152/293 不变。
4. 数据健康品牌蓝：dh-seg 在期/dh-bar/bs-dot ok → --accent；warn/bad 仍 amber/red。闸走 color-mix 探针（color(srgb…) 序列化两侧同路才可比；探针读完再 remove）。
5. 决策面板重构（去大字报第二层=构图）：≥1024 grid-areas 双栏（左判词+要点 / 右硬闸），眉标并一行、时间戳落句下、要点行引文缩进；DOM 零改、19px 字号不动；地板 296/506/564（横幅日扫宽上界+2）。
6. 黄金：portfolio.json gold_dca 实证已回本（avg_cost 3.3538 < nav 3.3774，+0.7%，08-24）。展示层按 aboveWater 分档 + 修「+-0.70%」双符号 + 摊薄文案数据驱动（越线后定投=成本线上移）。**策略层只出建议未动参数**（结论已交 kcn：倾向继续 ¥200/日、不加止盈、加码留给回撤）。

验证：浏览器 spec 全绿（含 3 条新闸）、parity 棘轮 8 项不变、定向 pytest 148 passed；CLS A/B×3 同 harness 同数据 1200 0.0117→0.0067 / 1440 0.0136→0.0056 / 390 持平。

坑：/root/node_modules 的 playwright 被别的 npm install 清掉（会话中途 MODULE_NOT_FOUND）→ `npm install --no-save playwright@1.60.0` 恢复（1.60 才配 chromium-1223）。无头外壳两次被孤儿回收器误杀，同会话 resume 后按 git status 断点续跑。
