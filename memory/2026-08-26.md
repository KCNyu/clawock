
## GIF 发灰根因修复 + repo 盘库（17:2x HKT 交互会话）

- kcn 问 hero GIF 为什么偏灰。实测两层：①管线主因——MEDIANCUT 按像素量分箱，~205/256 席给了不可分辨浅白，`#36A3FF` 最近距离 105（08-24 旧图同样烂，非 #1027 回归）；②次因——#1002 去蓝后整页更中性放大观感。修法=种子调色板（CSS/JS 扫 chroma>40，含 echarts 默认色板），距离 80→0、体积 −5%。issue #1030 → PR #1031 全绿自合并 → refresh_live 落地 → 手动 dispatch 重拍，新 GIF 已直推 master。判据与坑记进 shared-memory `clawock-gif-palette-accent-starvation.md`。
- 盘库：已清可再生缓存 ~13M（__pycache__/.pytest_cache/build/）；.clawhub 197M、插件 node_modules 155M、memory/.tmp 37M 等列了清单等 kcn 挑。注意：docs/visual-regression 的 jpg 是契约测试 fixtures，不是垃圾。
